import React from "react";
import { stackList } from "../../data/ProjectData";
import {
  Image,
  Technologies,
  Tech,
  TechImg,
  TechName,
  ContactWrapper,
} from "./AboutElements";
function About() {
  return (
    <ContactWrapper id="about">
      <div className="Container">
        <div className="SectionTitle">About Me</div>
        <div className="BigCard">
          <Image
            src="/Assets/se.png"
            alt="Display Picture"
          />
          <div className="AboutBio">
            Hello! My name is <strong>Ratul Hasan</strong> A software engineer with a passion to develop innovative and creative software solutions for entire software development life cycle. Having keen interest in research and analysis, intend to work in lively atmosphere where dynamism gets a preference and always believe in goal congruence for both individual and Organizational level.
          </div>
          <div className="AboutBio tagline2">
            I have become confident using the following technologies.
          </div>
          <Technologies>
            {stackList.map((stack, index) => (
              <Tech key={index} className="tech">
                <TechImg src={stack.img} alt={stack.name} />
                <TechName>{stack.name}</TechName>
              </Tech>
            ))}
          </Technologies>
        </div>
      </div>
    </ContactWrapper>
  );
}

export default About;
